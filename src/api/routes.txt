const allMovies = await axios.get("https://api.themoviedb.org/3/movie/popular", {//a faireà init dans homepage.tsx
  params: {
    api_key: process.env.REACT_APP_API_KEY,
  },
});

const movieByCategories = await axios.get(
  "https://api.themoviedb.org/3/discover/movie",//  /categoryName when filter clicked
  {
    params: {
      api_key: process.env.REACT_APP_API_KEY,
      with_genres: GENRE_ID, //id du la categorie desiree
      
    },
  }
);

const movieById = await axios.get(
  `https://api.themoviedb.org/3/movie/${MOVIE_ID}`,//movie id dans url et dans detais recup url id
  {
    params: {
      api_key: process.env.REACT_APP_API_KEY,
    },
  }
);

const basedOnSearch = await axios.get("https://api.themoviedb.org/3/search/movie", {
  params: {
    api_key: process.env.REACT_APP_API_KEY,
    query: SEARCH_QUERY,//with the desired search term
  },
});

const allCategories = await axios.get(
  "https://api.themoviedb.org/3/genre/movie/list", //init dans filter
  {
    params: {
      api_key:process.env.REACT_APP_API_KEY,
    },
  }
);
